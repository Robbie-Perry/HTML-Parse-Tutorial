# Uncomment the next line to define a global platform for your project
# platform :ios, '10.3'

target 'HTML Parse' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for HTML Parse
  pod 'Alamofire', '~> 4.6'
  pod 'TraceLog'
  pod 'SwiftSoup'
end
